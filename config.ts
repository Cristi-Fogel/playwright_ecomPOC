// currently used to pick up just baseURLs for tests
// can be set up for environment as well

export const baseURLs = {
  ecomURL:             "https://magento.softwaretestingboard.com"
};

export const paths = {
  home:                 "/",
  customerLoginPage:    "/customer/account/login/"
};
